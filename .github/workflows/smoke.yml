name: CI — Smoke
on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from Actions! ref=${{ github.ref }} event=${{ github.event_name }}"
